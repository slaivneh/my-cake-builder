/*
API giỏ hàng

- Thêm
- Sửa
- Xóa
- Lấy giỏ hàng
*/
import axiosClient from "./axiosClient";

const cartService = {
  getByUser(userId) {},

  addItem(item) {},

  updateItem(id, data) {},

  removeItem(id) {},

  clearCart(userId) {},
};

export default cartService;
