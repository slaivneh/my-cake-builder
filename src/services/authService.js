/*
API đăng nhập

Bao gồm:
- Login
- Register
- Logout
*/
import axiosClient from "./axiosClient";

const authService = {
  login(email, password) {},

  register(user) {},

  logout() {},
};

export default authService;
