/*
API địa chỉ giao hàng
*/
import axiosClient from "./axiosClient";

const addressService = {
  getByUser(userId) {},

  add(address) {},

  update(id, address) {},

  remove(id) {},
};

export default addressService;
